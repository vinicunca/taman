/**
 * This is a workaround since app.config is not capable to get the options from a module,
 * so we need to hardcode it.
 */
export const BRANDS = ['primary', 'secondary', 'success', 'info', 'warning', 'error'];
