# Implementation Plans

Generated by the improve skill on 2026-09-21. Execute in the order below. Each executor must read the plan fully, honor its STOP conditions, and update the status row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Mount imperative alerts through Pohon's in-app overlay host | P1 | M | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- None. This plan deliberately leaves `useTamanDialog` unchanged: it continues to own declared, stateful feature dialogs.

## Verification exception

- Plan 001's focused alert suite passed (3 files, 11 tests). The planned `pnpm check:type` could not start at the `e8f1659` baseline because Turbo configuration is absent; a direct popup-ui `vue-tsc` check also reported only pre-existing errors outside this plan's scope. The implementation was approved after diff, scope, and focused-test review.

## Findings considered and rejected

- Replace `useTamanDialog` with Pohon's `useOverlay`: rejected. `useTamanDialog` supplies typed shared data, store-backed state, drag/fullscreen behavior, and view-owned lifecycle that one-shot alerts and prompts do not need.
- Change the public cancellation contract in this migration: deferred. Returning `boolean` / `undefined` instead of rejecting on user cancellation is desirable, but mixing it with the rendering migration would require a separate call-site and compatibility decision.
